import { CheckboxInput, type FeatureToggle } from '../base';

export const auto_fit_viewport: FeatureToggle = {
  name: 'Автоподгонка области обзора',
  category: 'UI',
  component: CheckboxInput,
};
